# Environment files — never commit secrets
.env
.env.local
.env.production
.env.staging
env.local
env.production
env.staging
server/.env
server/.env.local
server/.env.production
server/.env.staging
server/env.local
server/env.production
server/env.staging

# Large/generated files
Resumes/
evals/fixtures/resumes/

# Marketing automation scripts (contain local keys, not part of web app)
instagram_analysis.py
assemblyai.py
apify_reels.py
apify_posts.py
transcription_checkpoint.json
instagram_analysis.xlsx

# Git worktrees
.worktrees/
.claude/worktrees/
screenshots/

# Open Design workspace (full repo clone)
.open-design/
.open-design-temp/
Report/
server/scripts/qa_report.md
server/scripts/qa_run_output.log
plan.md

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# marketing capture output
marketing/
.capture-profile/

# sponsor registry source documents (re-downloadable, see sponsor-source/README.md)
server/data/sponsor-source/*.pdf
server/data/sponsor-source/*.zip
# intermediates, all regenerable for free by re-running the pipeline
server/data/sponsor-source/abr_matches.json
server/data/sponsor-source/sponsors_merged.json
server/data/sponsor-source/sponsor_table_backup_*.json
# sponsor_registry.json IS committed: it carries the paid classification pass, so
# regenerating it costs money rather than just time.
